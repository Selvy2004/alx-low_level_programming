#include "main.h"

/**
 * _abs - checks for alphabetic character
 *
 * @n: take the input from another
 *
 * Return: 0 (Success)
 *
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
